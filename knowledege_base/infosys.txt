Record TypeCompany Specific QuestionCompany  InfosysRole  Java DeveloperCategory  TechnicalExperience Level  Fresher, ExperiencedDifficulty Level  Medium---QuestionExplain the core Object‑Oriented Programming (OOP) concepts in Java and illustrate each concept with a practical, real‑world example that a Java developer might encounter on the job.---Model AnswerOOP Concept	Definition (Java)	Real‑Time / Business Example1. Encapsulation	Bundles data (fields) and the methods that operate on that data into a single class. Access to the internal state is controlled through access modifiers (`private`, `protected`, `public`) and accessor methods (getters/setters).	Bank Account – `class BankAccount` has private fields `accountNumber`, `balance`. Public methods `deposit()`, `withdraw()`, `getBalance()` expose only safe operations, preventing external code from arbitrarily changing the balance.2. Abstraction	Hides complex implementation details and exposes only the essential features via abstract classes or interfaces. Clients work with high‑level contracts without needing to know how they are fulfilled.	Payment Service – `interface PaymentProcessor { boolean process(PaymentRequest req); }`. Different providers (`PayPalProcessor`, `StripeProcessor`) implement the interface. The rest of the application calls `processor.process(req)` without caring whether PayPal or Stripe is used.3. Inheritance	Allows a new class (subclass) to acquire the fields and methods of an existing class (superclass), promoting code reuse and establishing an is‑a relationship.	Vehicle hierarchy – `class Vehicle { String make; String model; void startEngine(); }` → `class Car extends Vehicle { int numberOfDoors; }`. `Car` inherits common vehicle behavior while adding car‑specific attributes.4. Polymorphism	The ability of a single interface to represent different underlying data types. In Java this appears as method overriding (runtime polymorphism) and method overloading (compile‑time polymorphism).	Logging – `Logger logger = LoggerFactory.getLogger(MyClass.class);`<br>At runtime the actual object may be a `ConsoleLogger`, `FileLogger`, or `RemoteLogger`. The call `logger.log("msg")` invokes the concrete implementation appropriate for the environment.5. Composition (has‑a relationship) (often discussed together with OOP)	Instead of inheriting, a class can contain instances of other classes, delegating responsibilities. This encourages loose coupling.	Order processing – `class Order { List<OrderItem> items; PaymentProcessor paymentProcessor; }`. `Order` has a collection of `OrderItem` objects and has a `PaymentProcessor`. Changing the payment method does not affect `Order`’s core logic.Brief Code Illustration (All concepts together)```java// 1. Encapsulationpublic class BankAccount {    private final String accountNumber;    private double balance;    public BankAccount(String accNo, double initBal) {        this.accountNumber = accNo;        this.balance = initBal;    }    public double getBalance() { return balance; }    // 4. Polymorphism (method overriding)    public void deposit(double amount) { balance += amount; }    public void withdraw(double amount) { balance -= amount; }}// 2. Abstractionpublic interface PaymentProcessor {    boolean process(double amount);}// 3. Inheritance & 4. Polymorphismpublic class PayPalProcessor implements PaymentProcessor {    @Override    public boolean process(double amount) {        // PayPal‑specific API call        return true;    }}// 5. Compositionpublic class OnlinePurchase {    private final BankAccount payerAccount;    private final PaymentProcessor paymentProcessor; // composed    public OnlinePurchase(BankAccount acct, PaymentProcessor processor) {        this.payerAccount = acct;        this.paymentProcessor = processor;    }    public boolean pay(double amount) {        if (paymentProcessor.process(amount)) {            payerAccount.withdraw(amount);            return true;        }        return false;    }}```---Key ConceptsEncapsulation protects internal state → maintain invariants, easier debugging.Abstraction defines clear contracts → interchangeable implementations, better testability.Inheritance promotes reuse but should be used judiciously; favor composition when “has‑a” fits better.Polymorphism enables writing code against super‑type references, allowing runtime behavior to vary without code changes.Composition leads to loosely coupled designs, facilitating unit testing and future extensions.---Evaluation PointsCriterion	What the interviewer looks forClear definition of each OOP principle.	Ability to articulate concepts in plain language.Correct Java terminology (access modifiers, interface, abstract class, overriding, overloading, inheritance hierarchy).	Demonstrates familiarity with Java syntax and semantics.Real‑world example that maps directly to the principle.	Shows practical understanding, not just theory.Code snippet that combines the concepts cohesively.	Ability to translate ideas into working Java code.Awareness of design trade‑offs (e.g., when to prefer composition over inheritance).	Indicates good software‑design sense.---Common MistakesMistake	Why it’s wrong / how to fixMixing abstraction with encapsulation (e.g., saying “encapsulation hides implementation details”).	Encapsulation is about data hiding; abstraction is about exposing a contract. Keep them distinct.Using inheritance for every relationship (e.g., `class Employee extends Person` and `class Manager extends Employee`).	Over‑using inheritance leads to fragile hierarchies; prefer composition when the relationship is “has‑a”.Providing public fields instead of private fields with getters/setters.	Breaks encapsulation and makes the class vulnerable to invalid states.Ignoring method overloading as part of polymorphism.	Overloading is compile‑time polymorphism; mention it along with overriding.Forgetting to mention interface‑based programming as a primary way to achieve abstraction in Java.	Interfaces are the cornerstone of Java abstraction; they enable multiple implementations.---Follow‑up QuestionsWhen would you choose an abstract class over an interface (or vice‑versa) in Java 8+?Explain the “Liskov Substitution Principle” and give a Java example where violating it can cause bugs.How does Java’s `final` keyword relate to OOP concepts such as inheritance and polymorphism?Describe a scenario where composition is preferable to inheritance and rewrite a small class diagram to reflect that change.What are the advantages of using design patterns (e.g., Strategy, Factory) that rely heavily on OOP principles?---Interview TipsStart with a one‑sentence definition for each principle before diving into examples.Tie each concept to a business domain (banking, e‑commerce, payments) – Infosys often evaluates domain‑aware thinking.Show the code (even a short snippet) to prove you can implement the idea, not just talk about it.Mention best practices (e.g., favor composition, keep interfaces small, avoid exposing mutable internal state).If asked about Java 8 features, bring up default methods in interfaces as a way to add behavior while preserving abstraction.---Tags  Infosys, OOP, Java, Encapsulation, Abstraction, Inheritance, Polymorphism, Composition, Medium, Fresher, ExperiencedSource  Candidate Interview Experience (Infosys Java Developer interview) & standard OOP literature (Effective Java, Java SE Documentation).Record TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelEasyQuestionWhat is the difference between `==` and `.equals()` when comparing two `String` objects in Java?Model Answer`==` compares reference equality – it checks whether both variables point to the exact same object in memory.`.equals()` (for `String`) compares value equality – it checks whether the sequences of characters inside the two `String` objects are identical.Example:java String a = new String("Info"); String b = new String("Info");System.out.println(a == b); // false (different objects) System.out.println(a.equals(b)); // true (same characters)Key ConceptsReference vs. value comparison.`String` overrides `Object.equals()` to perform character‑by‑character comparison.Evaluation PointsCorrectly state that `==` checks memory address, `.equals()` checks logical content.Provide a short code example demonstrating both results.Common MistakesAssuming `==` works for `String` content comparison.Forgetting that `.equals()` can be `null`‑safe if called on a constant (`"Info".equals(a)`).Follow‑up QuestionsHow does `String.intern()` affect `==` comparisons?What is the contract of `hashCode()` when overriding `equals()`?How would you compare two custom objects for logical equality?SourceCandidate interview experiences at Infosys; Oracle Java SE Documentation.TagsInfosys, Java, Equality, String, Easy, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelEasyQuestionExplain the four core Object‑Oriented Programming (OOP) concepts in Java and give a brief real‑world example for each.Model AnswerOOP Concept	Definition (Java)	Real‑World ExampleEncapsulation	Bundles data and methods in a class; hides internal state using `private` fields and public getters/setters.	`BankAccount` class with private `balance` and public `deposit()` / `withdraw()`.Abstraction	Exposes only essential features via interfaces or abstract classes.	`PaymentProcessor` interface with implementations `PayPalProcessor`, `StripeProcessor`.Inheritance	A subclass inherits fields/methods of a superclass (`extends`).	`Car extends Vehicle`.Polymorphism	One interface, many implementations; achieved via method overriding (runtime) and overloading (compile‑time).	`Logger logger = new FileLogger(); logger.log(msg);` calls `FileLogger.log`.Key ConceptsEncapsulation protects state.Abstraction defines contracts.Inheritance promotes reuse; use it judiciously.Polymorphism enables interchangeable implementations.Evaluation PointsClear definition of each concept.Correct Java terminology (`private`, `interface`, `extends`, `@Override`).Practical example that maps directly to the principle.Common MistakesMixing up abstraction with encapsulation.Using public fields instead of getters/setters.Follow‑up QuestionsWhen would you prefer composition over inheritance?How does Java 8’s default methods affect interface abstraction?Explain the Liskov Substitution Principle with a Java example.SourceInfosys Java Developer interview feedback; “Effective Java” (Joshua Bloch).TagsInfosys, Java, OOP, Encapsulation, Inheritance, Polymorphism, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionYou need to find the most frequent element in a large integer array. Which Java Collection would you use and how would you implement it?Model AnswerUse a `HashMap<Integer, Integer>` to store the frequency of each element.java public int mostFrequent(int[] arr) { Map freq = new HashMap<>(); int maxCount = 0; int result = arr[0];for (int num : arr) {int count = freq.getOrDefault(num, 0) + 1;freq.put(num, count);    if (count &gt; maxCount) {        maxCount = count;        result = num;    }}return result;}ComplexitiesMetric	ValueTime	O(n) – one pass through the array.Space	O(k) where k is the number of distinct elements (≤ n).Key Concepts`HashMap` provides O(1) average insert & lookup.`getOrDefault` simplifies the counting logic.Evaluation PointsCorrect use of `HashMap`.Proper handling of ties (optional – can return first encountered).Mention of time/space complexities.Common MistakesUsing a `List` and `contains` → O(n²) time.Forgetting to update `maxCount` when a new maximum is found.Follow‑up QuestionsHow would you modify the solution to return all elements that share the highest frequency?Could you solve the problem with O(1) extra space if the array values are within a known small range?What Java 8 stream API could be used to achieve the same result?SourceInfosys Java interview experiences; Java Collections Framework documentation.TagsInfosys, Java, Collections, HashMap, Frequency, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionExplain the difference between checked and unchecked exceptions in Java. When would you create a custom checked exception?Model AnswerChecked Exceptions – Subclasses of `Exception` (except `RuntimeException`). The compiler forces the caller to either handle (`try/catch`) or declare (`throws`). Used for recoverable conditions (e.g., `IOException`, `SQLException`).Unchecked Exceptions – Subclasses of `RuntimeException`. The compiler does not require handling. Typically indicate programming errors (e.g., `NullPointerException`, `IllegalArgumentException`).Custom Checked Exception Examplejava public class InsufficientBalanceException extends Exception { public InsufficientBalanceException(String msg) { super(msg); } }Use case: In a banking API, if a withdrawal request exceeds the account balance, the method can `throw new InsufficientBalanceException(...)`; the client must decide how to handle it.Key ConceptsCompile‑time checking for checked exceptions.Runtime vs. recoverable error semantics.When to define a checked exception – for business‑logic errors that the caller is expected to handle.Evaluation PointsCorrect classification of exception hierarchy.Reasonable scenario for a custom checked exception.Sample code showing a custom checked exception class.Common MistakesSaying “checked exceptions are always better”; they can lead to noisy APIs.Forgetting to add `throws` clause on the method signature.Follow‑up QuestionsHow would you convert a checked exception to an unchecked one?What is the purpose of `Throwable`’s `addSuppressed` method?Explain the effect of `try‑with‑resources` on exception handling.SourceInfosys Java Developer interview notes; Oracle Java SE Documentation on Exceptions.TagsInfosys, Java, Exceptions, Checked, Unchecked, CustomException, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionWrite a thread‑safe singleton class in Java using the double‑checked locking pattern.Model Answerjava public class Singleton { // volatile prevents instruction re‑ordering private static volatile Singleton instance = null;private Singleton() {// private constructor}public static Singleton getInstance() {if (instance == null) {               // first check (no locking)synchronized (Singleton.class) {if (instance == null) {       // second check (with lock)instance = new Singleton();}}}return instance;}}Explanation:The first `if` avoids unnecessary synchronization after the instance is created.The `synchronized` block ensures only one thread can create the instance.`volatile` guarantees visibility of the initialized object to other threads.Key ConceptsLazy initialization.Double‑checked locking reduces synchronization overhead.`volatile` solves the half‑constructed object problem.Evaluation PointsUse of `private` constructor, `volatile` field, and two‑level null checks.Thread safety explained.Common MistakesOmitting `volatile` – can lead to subtle bugs on some JVMs.Synchronizing the whole `getInstance()` method (inefficient).Follow‑up QuestionsHow does the Enum Singleton approach compare?What would happen if the constructor throws an exception?Explain the difference between synchronization and java.util.concurrent.locks.Lock.SourceInfosys Java interview resources; “Effective Java” Item 3 (Singletons).TagsInfosys, Java, Singleton, Multithreading, DoubleCheckedLocking, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionGiven a list of `Employee` objects (`int id, String name, double salary, String department`), write a single Java Stream pipeline to produce a `Map<String, Double>` that contains, for each department, the average salary of employees whose salary is above the overall average salary.Model Answerjava double overallAvg = employees.stream() .mapToDouble(Employee::getSalary) .average() .orElse(0.0);Map deptAvg = employees.stream() .filter(e -> e.getSalary() > overallAvg) // keep only above‑overall‑avg .collect(Collectors.groupingBy( Employee::getDepartment, Collectors.averagingDouble(Employee::getSalary) ));StepsCompute overall average salary.Filter employees whose salary exceeds that average.Group the remaining employees by `department` and compute the average salary per group.Key Concepts`mapToDouble` → primitive stream for numeric aggregation.`average()` returns an `OptionalDouble`.`groupingBy` collector with downstream `averagingDouble`.Stateless intermediate operation (`filter`).Evaluation PointsCorrect two‑step approach (overall average first, then filter).Proper use of `Collectors.groupingBy` and `averagingDouble`.Handles empty list safely (`orElse(0.0)`).Common MistakesTrying to compute both averages in a single pipeline (impractical because the overall average is needed first).Using `Collectors.toMap` without handling duplicate keys.Follow‑up QuestionsHow would you modify the pipeline to return the top‑3 highest‑paid employees per department?Explain the difference between parallel streams and sequential streams in this context.What is the impact of using `Collectors.summingDouble` vs `averagingDouble`?SourceInfosys Java Developer interview notes; Oracle Java Stream API documentation.TagsInfosys, Java, Streams, Lambdas, Collectors, Map, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionExplain the Factory Method design pattern and implement it in Java to create different types of `Notification` objects (`EmailNotification`, `SMSNotification`, `PushNotification`). Include the client code that uses the factory.Model AnswerPattern Explanation  Factory Method defines an interface for creating an object, but lets subclasses decide which class to instantiate. It promotes loose coupling by moving object creation out of the client.Java Implementationjava // 1. Product interface public interface Notification { void send(String message); }// 2. Concrete products public class EmailNotification implements Notification { @Override public void send(String message) { System.out.println("Sending EMAIL: " + message); } }public class SMSNotification implements Notification { @Override public void send(String message) { System.out.println("Sending SMS: " + message); } }public class PushNotification implements Notification { @Override public void send(String message) { System.out.println("Sending PUSH: " + message); } }// 3. Creator (Factory) public abstract class NotificationFactory { public abstract Notification createNotification(); }// 4. Concrete factories public class EmailFactory extends NotificationFactory { @Override public Notification createNotification() { return new EmailNotification(); } }public class SMSFactory extends NotificationFactory { @Override public Notification createNotification() { return new SMSNotification(); } }public class PushFactory extends NotificationFactory { @Override public Notification createNotification() { return new PushNotification(); } }// 5. Client code public class NotificationService { private final NotificationFactory factory;public NotificationService(NotificationFactory factory) {this.factory = factory;}public void notifyUser(String msg) {Notification n = factory.createNotification();n.send(msg);}public static void main(String[] args) {NotificationService emailSvc = new NotificationService(new EmailFactory());emailSvc.notifyUser("Welcome to Infosys!");    NotificationService smsSvc = new NotificationService(new SMSFactory());    smsSvc.notifyUser("Your OTP is 123456");}}Key ConceptsProduct (`Notification`) defines the common interface.Creator (`NotificationFactory`) declares the factory method.Concrete Creators (`EmailFactory`, `SMSFactory`, `PushFactory`) override the method to instantiate specific products.Client depends only on the abstract `NotificationFactory`, not on concrete classes.Evaluation PointsClear description of the pattern’s intent and benefits.Correct hierarchy (Product, Concrete Products, Creator, Concrete Creators).Working client code that demonstrates dependency injection of the factory.Common MistakesReturning concrete types directly from the factory (breaks abstraction).Mixing up Factory Method with Abstract Factory (the latter creates families of related objects).Follow‑up QuestionsHow would you refactor the code using the Abstract Factory pattern if you also needed to create `NotificationSender` objects?Discuss the advantages of using Dependency Injection (e.g., Spring) with this pattern.How does the pattern help with unit testing?SourceInfosys Java interview material; “Design Patterns: Elements of Reusable Object‑Oriented Software” (GoF).TagsInfosys, Java, Design Pattern, Factory Method, OOP, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionExplain what a deadlock is in Java multithreading. Provide a code example that can cause a deadlock and show how to resolve it using a consistent lock‑ordering strategy.Model AnswerDeadlock definition  A deadlock occurs when two or more threads are each waiting for a lock held by another thread, resulting in a cycle of dependencies where none can proceed.Problematic codejava class Account { private final int id; private int balance; public Account(int id, int bal) { this.id = id; this.balance = bal; }public int getId() { return id; }// transfer money from this account to anotherpublic void transfer(Account to, int amount) {synchronized (this) {System.out.println(Thread.currentThread().getName() + " locked A" + id);synchronized (to) {System.out.println(Thread.currentThread().getName() + " locked A" + to.id);this.balance -= amount;to.balance += amount;}}}}// Demo that deadlocks public class DeadlockDemo { public static void main(String[] args) { Account acc1 = new Account(1, 1000); Account acc2 = new Account(2, 1000);    Thread t1 = new Thread(() -> acc1.transfer(acc2, 100), "T1");    Thread t2 = new Thread(() -> acc2.transfer(acc1, 200), "T2");    t1.start();    t2.start();}}Thread T1 locks `acc1` then tries to lock `acc2`.  Thread T2 locks `acc2` then tries to lock `acc1`.  If both acquire their first lock, they wait forever → deadlock.Resolution – Consistent lock orderingjava public void transfer(Account to, int amount) { Account first = this.id < to.id ? this : to; Account second = this.id < to.id ? to : this;synchronized (first) {synchronized (second) {this.balance -= amount;to.balance += amount;}}}By always acquiring locks in the same order (`lower id` first), the circular wait condition is eliminated.Key ConceptsFour necessary conditions for deadlock: mutual exclusion, hold‑and‑wait, no preemption, circular wait.Breaking circular wait via global lock ordering or using `java.util.concurrent` locks with `tryLock` and timeout.Evaluation PointsAccurate definition of deadlock.Minimal reproducible deadlock code.Correct fix using consistent ordering (or alternative using `tryLock`).Common MistakesAssuming `synchronized` automatically prevents deadlocks.Forgetting to order the locks; simply swapping `synchronized` blocks does not help.Follow‑up QuestionsHow would you use `java.util.concurrent.locks.ReentrantLock` with `tryLock` to avoid deadlock?What Java tool can help you detect deadlocks at runtime? (`jstack`, VisualVM).Explain the difference between liveness and safety properties in concurrent programs.SourceInfosys Java concurrency interview notes; Java Concurrency in Practice (Brian Goetz).TagsInfosys, Java, Concurrency, Deadlock, Synchronization, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionDescribe the Java heap memory layout (young, old generations) and explain how a Full GC differs from a Minor GC. Also, list two JVM flags you would use to tune GC for a low‑latency, high‑throughput application.Model AnswerHeap layout+-------------------+ | Young Generation| | +---+ +-------+ | | |Eden| |Survivor| | | +---+ +-------+ | +-------------------+ | Old (Tenured) | +-------------------+ | Metaspace | +-------------------+Eden – where new objects are allocated.Survivor spaces (S0, S1) – hold objects that survived one or more Minor GCs.Old/Tenured – holds long‑lived objects promoted after several survivals.GC TypesType	Triggers	What is collected	Typical pause timeMinor GC	When Eden fills up.	Only young generation (Eden + one Survivor).	Usually sub‑millisecond to a few ms.Full GC (or Major GC)	When old generation fills or explicit `System.gc()`.	Entire heap (young + old) and possibly Metaspace.	Can be tens to hundreds of ms; can stop the world.Tuning flags (example for G1 GC, low‑latency)`-XX:MaxGCPauseMillis=200` – target maximum pause time of 200 ms.`-XX:InitiatingHeapOccupancyPercent=45` – start concurrent cycle when heap is 45 % full (helps avoid long Full GCs).For high‑throughput (throughput‑oriented) you might use:`-XX:+UseParallelGC` – parallel young‑generation collector.`-XX:ParallelGCThreads=<num>` – number of threads for parallel phases.Key ConceptsYoung vs. Old generation – generational hypothesis.Stop‑the‑World pauses differ between Minor and Full GC.GC tuning via `-XX` flags to meet latency or throughput goals.Evaluation PointsCorrect diagram/description of heap regions.Distinguish when Minor vs. Full GC runs and their impact.Provide at least two realistic JVM tuning flags.Common MistakesClaiming that `System.gc()` always triggers a Full GC (it’s a request, not a guarantee).Ignoring the role of Survivor spaces in promotion.Follow‑up QuestionsHow does G1 GC differ from Parallel GC in terms of region layout?What is the purpose of the Metaspace, and how do you limit its size?Explain how CMS (Concurrent Mark‑Sweep) works and why it is deprecated in newer JDKs.SourceInfosys Java Developer interview notes; Oracle JDK HotSpot VM documentation.TagsInfosys, Java, JVM, Garbage Collection, G1, Full GC, Minor GC, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleJava DeveloperCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionIn a Spring Boot application, you need to ensure that a service method updates two database tables atomically. Explain how you would achieve this using Spring Transaction Management, and describe what happens if a RuntimeException is thrown inside the method.Model AnswerImplementationjava @Service public class OrderService {private final OrderRepository orderRepo;private final InventoryRepository inventoryRepo;@Autowiredpublic OrderService(OrderRepository orderRepo,InventoryRepository inventoryRepo) {this.orderRepo = orderRepo;this.inventoryRepo = inventoryRepo;}@Transactional   // <‑‑ enables declarative transactionpublic void placeOrder(OrderDto dto) {Order order = orderRepo.save(dto.toOrder());          // writes to ORDER tableinventoryRepo.decreaseStock(dto.getProductId(),dto.getQuantity());       // writes to INVENTORY table// If any RuntimeException occurs below, the transaction will be rolled back}}What happens on a `RuntimeException`?The `@Transactional` interceptor opens a transaction before entering `placeOrder`.If a unchecked exception (`RuntimeException` or subclass) propagates out of the method, Spring marks the transaction for rollback.When the method exits, the transaction manager rolls back all changes made to both tables, leaving the database in its original state.Checked exceptions do not trigger automatic rollback unless `rollbackFor` is specified.Key points to configureEnsure a transaction manager bean (`DataSourceTransactionManager` for JDBC or `JpaTransactionManager` for JPA) is present.Use propagation = Propagation.REQUIRED (default) so the method joins an existing transaction if present.Key ConceptsDeclarative transaction via `@Transactional`.Default rollback rule for unchecked exceptions.Propagation and isolation levels (optional discussion).Evaluation PointsCorrect annotation usage and explanation of its effect.Clear description of automatic rollback on `RuntimeException`.Mention of transaction manager configuration.Common MistakesBelieving that checked exceptions automatically cause rollback.Forgetting to make the service method `public` (Spring AOP proxies only public methods).Follow‑up QuestionsHow would you force a rollback on a checked exception? (`@Transactional(rollbackFor = MyCheckedException.class)`)Explain the difference between REQUIRED and REQUIRES_NEW propagation.What are the consequences of using `@Transactional` on a private method?SourceInfosys Spring Boot interview experiences; Spring Framework Reference – Transaction Management.TagsInfosys, Java, Spring, Transaction, @Transactional, RuntimeException, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelEasyQuestionWhat is the difference between `==` and `.equals()` when comparing two `String` objects in Java?Model Answer`==` checks reference equality – whether both variables point to the same object in memory.`.equals()` (overridden in `String`) checks value equality – whether the sequences of characters inside the two strings are identical.java String a = new String("Infosys"); String b = new String("Infosys");System.out.println(a == b); // false – different objects System.out.println(a.equals(b)); // true – same charactersKey ConceptsReference vs. value comparison.`String` overrides `Object.equals()` to perform character‑by‑character comparison.Evaluation PointsCorrectly state that `==` compares memory addresses, `.equals()` compares contents.Provide a concise code example illustrating both outcomes.Common MistakesAssuming `==` works for content comparison of strings.Forgetting null‑safe comparison (`"Infosys".equals(a)`).Follow‑up QuestionsHow does `String.intern()` affect `==` comparisons?What is the contract between `equals()` and `hashCode()`?How would you compare two custom objects for logical equality?SourceInfosys Java Developer interview experiences; Oracle Java SE Documentation.TagsInfosys, Java, Equality, String, Easy, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelEasyQuestionList and briefly explain the four fundamental Object‑Oriented Programming (OOP) concepts in Java, providing a simple real‑world example for each.Model AnswerOOP Concept	Definition (Java)	Real‑World ExampleEncapsulation	Bundles data and methods; hides internal state using `private` fields and public getters/setters.	`BankAccount` class with private `balance` and public `deposit()`/`withdraw()`.Abstraction	Exposes only essential features via interfaces or abstract classes.	`PaymentProcessor` interface with implementations `PayPalProcessor`, `StripeProcessor`.Inheritance	Subclass (`extends`) inherits fields/methods from a superclass.	`Car extends Vehicle`.Polymorphism	One interface, many implementations; achieved via method overriding (runtime) and overloading (compile‑time).	`Logger logger = new FileLogger(); logger.log(msg);` calls `FileLogger.log`.Key ConceptsEncapsulation protects state.Abstraction defines contracts.Inheritance enables reuse; use sparingly.Polymorphism allows interchangeable implementations.Evaluation PointsClear definition of each principle with correct Java terminology.Practical example that maps directly to the principle.Common MistakesMixing abstraction with encapsulation.Exposing public fields instead of using getters/setters.Follow‑up QuestionsWhen would you choose composition over inheritance?How do Java 8 default methods affect interface abstraction?Explain the Liskov Substitution Principle with a Java example.SourceInfosys Java interview notes; “Effective Java” (Joshua Bloch).TagsInfosys, Java, OOP, Encapsulation, Inheritance, Polymorphism, Easy, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionGiven an integer array, find the element that occurs most frequently. Which Java collection would you use and how would you implement the solution?Model AnswerUse a `HashMap<Integer, Integer>` to store frequencies.java public int mostFrequent(int[] arr) { Map freq = new HashMap<>(); int maxCount = 0; int result = arr[0];for (int num : arr) {int count = freq.getOrDefault(num, 0) + 1;freq.put(num, count);if (count > maxCount) {maxCount = count;result = num;}}return result;}ComplexitiesMetric	ValueTime	O(n) – single pass.Space	O(k) where k is the number of distinct elements (≤ n).Key Concepts`HashMap` gives O(1) average insert and lookup.`getOrDefault` simplifies counting logic.Evaluation PointsCorrect use of `HashMap`.Proper updating of the max‑frequency element.Mention of time & space complexities.Common MistakesUsing a list and `contains` → O(n²) time.Forgetting to update `maxCount` when a new maximum is found.Follow‑up QuestionsHow would you return all elements that share the highest frequency?If the value range is limited (e.g., 0‑255), could you solve it with O(1) extra space?Show how to achieve the same result with Java 8 streams.SourceInfosys Java interview experiences; Java Collections Framework documentation.TagsInfosys, Java, Collections, HashMap, Frequency, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionExplain the difference between checked and unchecked exceptions in Java. Provide an example of a custom checked exception you might create in a banking application.Model AnswerChecked Exceptions – Subclasses of `Exception` (excluding `RuntimeException`). The compiler forces the caller to either handle them (`try/catch`) or declare them (`throws`). Used for recoverable conditions (e.g., `IOException`, `SQLException`).Unchecked Exceptions – Subclasses of `RuntimeException`. No compile‑time enforcement; they usually indicate programming errors (e.g., `NullPointerException`, `IllegalArgumentException`).Custom Checked Exception Examplejava public class InsufficientBalanceException extends Exception { public InsufficientBalanceException(String msg) { super(msg); } }Use case: In a method `withdraw(double amount)`, if `amount > balance` the method throws `InsufficientBalanceException`. The caller must decide how to handle the situation.Key ConceptsCompile‑time checking for checked exceptions.Runtime vs. recoverable error semantics.When to define a checked exception – for business‑logic errors that callers are expected to handle.Evaluation PointsAccurate classification of the exception hierarchy.Reasonable scenario for a custom checked exception with sample code.Common MistakesAssuming all exceptions should be checked.Forgetting the `throws` clause on the method signature.Follow‑up QuestionsHow can you convert a checked exception into an unchecked one?What is the purpose of `Throwable.addSuppressed`?Explain how try‑with‑resources simplifies exception handling.SourceInfosys Java Developer interview notes; Oracle Java SE Documentation on Exceptions.TagsInfosys, Java, Exceptions, Checked, Unchecked, CustomException, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelMediumQuestionWrite a thread‑safe singleton class in Java using the double‑checked locking pattern.Model Answerjava public final class Singleton { private static volatile Singleton instance = null; // volatile prevents reorderingprivate Singleton() { /* private constructor */ }public static Singleton getInstance() {if (instance == null) {               // first check (no locking)synchronized (Singleton.class) {if (instance == null) {       // second check (with lock)instance = new Singleton();}}}return instance;}}ExplanationThe outer `if` avoids synchronization after the instance is created.The inner `synchronized` block guarantees that only one thread creates the instance.`volatile` ensures visibility of the fully constructed object to all threads.Key ConceptsLazy initialization.Double‑checked locking reduces synchronization overhead.`volatile` solves the half‑constructed object problem.Evaluation PointsPresence of `private` constructor, `volatile` field, and two null checks.Explanation of why each part is required.Common MistakesOmitting `volatile` – can lead to subtle bugs on some JVMs.Synchronizing the whole `getInstance()` method (inefficient).Follow‑up QuestionsCompare this approach with the Enum Singleton pattern.What happens if the constructor throws an exception?How does `java.util.concurrent.locks.Lock` differ from `synchronized` in this context?SourceInfosys Java interview resources; “Effective Java” Item 3 (Singletons).TagsInfosys, Java, Singleton, Multithreading, DoubleCheckedLocking, Medium, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionGiven a list of `Employee` objects (`int id, String name, double salary, String dept`), write a single Java Stream pipeline that returns a `Map<String, Double>` containing, for each department, the average salary of employees whose salary is above the overall average salary.Model Answerjava double overallAvg = employees.stream() .mapToDouble(Employee::getSalary) .average() .orElse(0.0);Map deptAvg = employees.stream() .filter(e -> e.getSalary() > overallAvg) // keep only above‑overall‑avg .collect(Collectors.groupingBy( Employee::getDepartment, Collectors.averagingDouble(Employee::getSalary) ));ExplanationCompute the overall average salary with `mapToDouble` and `average()`.Filter the list to keep only employees whose salary exceeds that average.Group the filtered employees by department and compute the average salary per group using `Collectors.averagingDouble`.Complexities  Time: O(n) – two passes over the list (one for overall average, one for grouping).  Space: O(k) for the resulting map where k = number of departments.Key ConceptsPrimitive streams (`mapToDouble`).`OptionalDouble.orElse` for empty collections.`Collectors.groupingBy` with a downstream `averagingDouble`.Evaluation PointsCorrect two‑step approach (overall average first, then filter).Proper use of `Collectors.groupingBy` and `averagingDouble`.Handles empty input safely.Common MistakesAttempting to compute both averages in a single pipeline (requires the overall average first).Using `toMap` without handling duplicate department keys.Follow‑up QuestionsHow would you modify the pipeline to return the top‑3 highest‑paid employees per department?Discuss the impact of converting the stream to parallel in this scenario.What would change if you needed the median salary per department instead of the average?SourceInfosys Java Developer interview notes; Oracle Java Stream API documentation.TagsInfosys, Java, Streams, Lambdas, Collectors, Map, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionExplain the Factory Method design pattern and implement it in Java to create different types of `Notification` objects (`EmailNotification`, `SMSNotification`, `PushNotification`). Include client code that uses the factory.Model AnswerPattern Overview  Factory Method defines an interface for creating an object, but lets subclasses decide which concrete class to instantiate. It promotes loose coupling by moving object creation out of the client.Java Implementationjava // 1. Product interface public interface Notification { void send(String message); }// 2. Concrete products public class EmailNotification implements Notification { @Override public void send(String msg) { System.out.println("EMAIL: " + msg); } }public class SMSNotification implements Notification { @Override public void send(String msg) { System.out.println("SMS: " + msg); } }public class PushNotification implements Notification { @Override public void send(String msg) { System.out.println("PUSH: " + msg); } }// 3. Creator (Factory) public abstract class NotificationFactory { public abstract Notification createNotification(); }// 4. Concrete factories public class EmailFactory extends NotificationFactory { @Override public Notification createNotification() { return new EmailNotification(); } }public class SMSFactory extends NotificationFactory { @Override public Notification createNotification() { return new SMSNotification(); } }public class PushFactory extends NotificationFactory { @Override public Notification createNotification() { return new PushNotification(); } }// 5. Client code public class NotificationService { private final NotificationFactory factory;public NotificationService(NotificationFactory factory) {this.factory = factory;}public void notifyUser(String msg) {Notification n = factory.createNotification();n.send(msg);}public static void main(String[] args) {NotificationService emailSvc = new NotificationService(new EmailFactory());emailSvc.notifyUser("Welcome to Infosys!");    NotificationService smsSvc = new NotificationService(new SMSFactory());    smsSvc.notifyUser("Your OTP is 456789");}}Key ConceptsProduct (`Notification`) – common interface.Creator (`NotificationFactory`) – declares the factory method.Concrete Creators (`EmailFactory`, `SMSFactory`, `PushFactory`) – instantiate specific products.Client depends only on the abstract factory, not on concrete classes.Evaluation PointsClear description of the pattern’s intent and benefits.Correct hierarchy (Product, Concrete Products, Creator, Concrete Creators).Working client code that demonstrates dependency injection of the factory.Common MistakesReturning concrete types from the factory (breaks abstraction).Confusing Factory Method with Abstract Factory (the latter creates families of related objects).Follow‑up QuestionsHow would you refactor to an Abstract Factory if you also needed to create `NotificationSender` objects?Discuss how Spring’s @Component and @Autowired can replace explicit factories.How does this pattern help with unit testing?SourceInfosys Java interview material; GoF “Design Patterns” (Factory Method).TagsInfosys, Java, Design Pattern, Factory Method, OOP, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionDefine a deadlock in Java multithreading. Provide a minimal code example that can cause a deadlock and show how to fix it using a consistent lock‑ordering strategy.Model AnswerDefinition  A deadlock occurs when two or more threads are each waiting for a lock held by another thread, forming a circular wait that prevents any of them from proceeding.Problematic codejava class Account { private final int id; private int balance;public Account(int id, int bal) { this.id = id; this.balance = bal; }public int getId() { return id; }// Transfer money from this account to anotherpublic void transfer(Account to, int amount) {synchronized (this) {System.out.println(Thread.currentThread().getName() + " locked A" + id);synchronized (to) {System.out.println(Thread.currentThread().getName() + " locked A" + to.id);this.balance -= amount;to.balance += amount;}}}}public class DeadlockDemo { public static void main(String[] args) { Account a1 = new Account(1, 1000); Account a2 = new Account(2, 1000);    Thread t1 = new Thread(() -> a1.transfer(a2, 100), "T1");    Thread t2 = new Thread(() -> a2.transfer(a1, 200), "T2");    t1.start();    t2.start();}}Thread T1 locks `a1` then tries `a2`.  Thread T2 locks `a2` then tries `a1`.  If both acquire their first lock, they wait forever → deadlock.Fix – Consistent lock orderingjava public void transfer(Account to, int amount) { Account first = this.id < to.id ? this : to; Account second = this.id < to.id ? to : this;synchronized (first) {synchronized (second) {this.balance -= amount;to.balance += amount;}}}By always acquiring locks in the same order (lowest `id` first), the circular‑wait condition is eliminated.Key ConceptsFour necessary conditions for deadlock: mutual exclusion, hold‑and‑wait, no preemption, circular wait.Breaking circular wait through global lock ordering (or using `tryLock` with timeout).Evaluation PointsAccurate definition of deadlock.Minimal reproducible deadlock code.Correct fix using consistent ordering (or mention of `ReentrantLock.tryLock`).Common MistakesAssuming `synchronized` alone prevents deadlocks.Forgetting to order the locks; simply swapping `synchronized` blocks does not help.Follow‑up QuestionsHow would you use `ReentrantLock.tryLock` to avoid deadlock?Which JVM tool can help you detect deadlocks at runtime? (`jstack`, VisualVM).Explain the difference between liveness and safety properties in concurrent programs.SourceInfosys Java concurrency interview notes; “Java Concurrency in Practice” (Brian Goetz).TagsInfosys, Java, Concurrency, Deadlock, Synchronization, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionDescribe the Java heap memory layout (young and old generations) and explain the difference between a Minor GC and a Full GC. Mention two JVM flags you would tune for a low‑latency, high‑throughput application.Model AnswerHeap Layout+-------------------+ | Young Generation | | +---+ +-----+ | | |Eden| |Survivor| | | +---+ +-----+ | +-------------------+ | Old (Tenured) | +-------------------+ | Metaspace | +-------------------+Eden – where new objects are allocated.Survivor spaces (S0, S1) – hold objects that survived at least one Minor GC.Old/Tenured – contains long‑lived objects promoted after several survivals.GC TypesType	When triggered	What is collected	Typical pause timeMinor GC	Eden fills up	Only young generation (Eden + one Survivor)	Usually sub‑millisecond to a few ms.Full GC (or Major GC)	Old generation fills, explicit `System.gc()`, or memory pressure	Entire heap (young + old) and possibly Metaspace	Can be tens to hundreds of ms, stop‑the‑world.Tuning Flags (example for G1 GC, low‑latency)`-XX:MaxGCPauseMillis=200` – target maximum pause time of 200 ms.`-XX:InitiatingHeapOccupancyPercent=45` – start a concurrent GC cycle when the heap reaches 45 % occupancy (helps avoid long Full GCs).For a high‑throughput focus you might use:`-XX:+UseParallelGC` – parallel young‑generation collector.`-XX:ParallelGCThreads=<num>` – number of threads for parallel phases.Key ConceptsGenerational hypothesis – most objects die young.Minor GC is cheap and frequent; Full GC is expensive and rare.GC tuning via `-XX` options to meet latency or throughput goals.Evaluation PointsCorrect description of heap regions and their purposes.Clear distinction between Minor and Full GC, including typical pause times.Provide at least two realistic JVM tuning flags.Common MistakesStating that `System.gc()` always forces a Full GC (it’s only a request).Ignoring the role of Survivor spaces in promotion.Follow‑up QuestionsHow does G1 GC differ from Parallel GC regarding region layout?What is the purpose of Metaspace, and how can you limit its size?Why has CMS been deprecated in newer JDK releases?SourceInfosys Java Developer interview notes; Oracle HotSpot VM documentation.TagsInfosys, Java, JVM, Garbage Collection, G1, Minor GC, Full GC, Hard, Fresher, ExperiencedRecord TypeCompany Specific QuestionCompanyInfosysRoleSoftware Developer / EngineerCategoryTechnicalExperience LevelFresher, ExperiencedDifficulty LevelHardQuestionIn a Spring Boot application, you need to guarantee that a service method updates two database tables atomically. Explain how you would achieve this using Spring Transaction Management, and describe what happens if a RuntimeException is thrown inside the method.Model AnswerImplementationjava @Service public class OrderService {private final OrderRepository orderRepo;private final InventoryRepository inventoryRepo;@Autowiredpublic OrderService(OrderRepository orderRepo,InventoryRepository inventoryRepo) {this.orderRepo = orderRepo;this.inventoryRepo = inventoryRepo;}@Transactional   // declarative transactionpublic void placeOrder(OrderDto dto) {Order order = orderRepo.save(dto.toOrder());                 // INSERT into ORDERinventoryRepo.decreaseStock(dto.getProductId(),dto.getQuantity());              // UPDATE INVENTORY// If any unchecked exception propagates, the transaction rolls back}}Behaviour on a `RuntimeException`Spring opens a transaction before entering `placeOrder`.If a unchecked exception (`RuntimeException` or subclass) propagates out of the method, Spring’s transaction interceptor marks the transaction for rollback.When the method exits, the transaction manager rolls back all changes made to both tables, leaving the database unchanged.Checked exceptions do not trigger automatic rollback unless `rollbackFor` is specified.Additional notesA `JpaTransactionManager` (or `DataSourceTransactionManager`) bean must be configured.Default propagation is `Propagation.REQUIRED` – the method joins any existing transaction or creates a new one.Key ConceptsDeclarative transaction via `@Transactional`.Default rollback rule for unchecked exceptions.Propagation and isolation levels (optional deeper discussion).Evaluation PointsCorrect use of `@Transactional` and explanation of its effect.Clear description of automatic rollback on `RuntimeException`.Mention of transaction manager configuration.Common MistakesBelieving that checked exceptions automatically cause rollback.Forgetting that the service method must be `public` for Spring AOP proxies to work.Follow‑up QuestionsHow would you force a rollback on a checked exception? (`@Transactional(rollbackFor = MyCheckedException.class)`)Explain the difference between REQUIRED and REQUIRES_NEW propagation.What happens if you place `@Transactional` on a private helper method?SourceInfosys Spring Boot interview experiences; Spring Framework Reference – Transaction Management.TagsInfosys, Java, Spring, Transaction, @Transactional, RuntimeException, Hard, Fresher, Experienced