Record TypeTechnical QuestionRole  Software Engineer / Backend Developer / Algorithm EngineerCategory  TechnicalSkill  Data Structures & AlgorithmsSubtopic  Search Algorithms – Binary SearchExperience Level  Fresher, ExperiencedDifficulty  Easy---QuestionExplain the binary search algorithm. What are its time and space complexities, and when can it be applied?---Model AnswerBinary search works on a sorted array (or any random‑access sequence).Set `low = 0`, `high = n‑1`.While `low ≤ high``mid = low + (high‑low)/2`.If `arr[mid] == target` → return `mid`.If `arr[mid] < target` → `low = mid + 1`.Else → `high = mid – 1`.If the loop ends, the element is not present (return –1 or insertion point).ComplexitiesMetric	ValueTime	O(log n) comparisons (each step halves the remaining interval).Space	O(1) for the iterative version; O(log n) recursion depth if implemented recursively.When applicableThe collection must be sorted (ascending or descending).Random access (indexable) data structures are needed – typical for arrays, `ArrayList`, or contiguous memory buffers.Not suitable for linked lists because you cannot jump to the middle in O(1).---Key ConceptsDivide‑and‑conquer – each iteration discards half of the remaining elements.Mid‑point calculation should avoid overflow (`low + (high‑low)/2`).Variants: lower‑bound, upper‑bound, binary search on Model Answer (search over a monotonic predicate).---Evaluation PointsCorrect iterative (or recursive) pseudocode.Mention that the array must be sorted and random‑access.State O(log n) time, O(1) space (iterative) and O(log n) space (recursive).---Common MistakesForgetting to update `low`/`high` correctly, leading to infinite loops.Using `(low + high)/2` without overflow protection.Trying binary search on an unsorted list.---Follow‑up QuestionsHow would you implement binary search on a rotated sorted array?What is the difference between lower‑bound and upper‑bound binary searches?How can you use binary search to find the square root of a number?---Interview TipsStart with a one‑sentence definition: “Binary search repeatedly halves a sorted range to locate a target in O(log n) time.”Emphasize the sorted‑data pre‑condition and the need for random access.Mention the overflow‑safe mid calculation as a common interview “gotcha”.---Tags  DSA, Binary Search, Search Algorithms, Time Complexity, Space Complexity, EasySourceCormen, Leiserson, Rivest, Stein – Introduction to Algorithms, 3rd ed., Chapter 9.https://en.wikipedia.org/wiki/Binary_search_algorithm---Record TypeTechnical QuestionRole  Software Engineer / Full‑Stack Developer / Algorithm EngineerCategory  TechnicalSkill  Data Structures & AlgorithmsSubtopic  Two‑Pointer TechniqueExperience Level  Fresher, ExperiencedDifficulty  Easy---QuestionGiven a sorted integer array, design an O(n) algorithm to determine whether any two numbers sum to a target value `k`. Explain the two‑pointer technique used.---Model AnswerAlgorithm (Two‑Pointer)text left = 0 right = n‑1 while left < right sum = arr[left] + arr[right] if sum == k → return true else if sum < k → left = left + 1 // need a larger sum else → right = right - 1 // need a smaller sum return falseWhy it worksBecause the array is sorted, increasing `left` makes the sum larger, decreasing `right` makes it smaller.Each iteration discards at least one element that cannot be part of a solution, guaranteeing O(n) time.ComplexityTime – O(n) (each pointer moves at most `n` steps).Space – O(1) (only two index variables).---Key ConceptsExploiting sorted order to avoid nested loops.Each element is examined at most once.Works for any monotonic property (e.g., `arr[left] + arr[right]`).---Evaluation PointsProvide the two‑pointer loop correctly.Explain why moving `left` or `right` is justified.State O(n) time, O(1) space.---Common MistakesUsing a nested loop (O(n²)) instead of two pointers.Forgetting to check `left < right` leading to self‑pairing.---Follow‑up QuestionsHow would you adapt the algorithm if the array is unsorted?Extend the idea to find all unique pairs that sum to `k`.How can the two‑pointer technique be used to solve the maximum water container problem?---Interview TipsMention that the array must be sorted; otherwise you sort first (O(n log n)) and then apply the technique.Emphasize that the method runs in linear time with constant extra space.---Tags  DSA, Two Pointers, Pair Sum, Sorted Array, Linear Time, EasySourceLeetCode problem “Two Sum II – Input array is sorted”.https://www.geeksforgeeks.org/two-pointers-technique/---Record TypeTechnical QuestionRole  Software Engineer / Backend Developer / Algorithm EngineerCategory  TechnicalSkill  Data Structures & AlgorithmsSubtopic  Graph Algorithms – Cycle Detection (Directed)Experience Level  Fresher, ExperiencedDifficulty  Medium---QuestionDescribe how to detect a cycle in a directed graph using depth‑first search. Include the role of vertex colors (WHITE, GRAY, BLACK) in your explanation.---Model AnswerAlgorithm (DFS with coloring)DFS-Visit(u): color[u] = GRAY // vertex is being explored for each v in Adj[u]: if color[v] == WHITE: if DFS-Visit(v) returns true → return true else if color[v] == GRAY: // back edge found → cycle return true color[u] = BLACK // finished exploring u return falseDetect-Cycle(G): for each vertex u in G: color[u] = WHITE for each vertex u in G: if color[u] == WHITE: if DFS-Visit(u) returns true → return true return false // no cycleExplanation of colorsColor	MeaningWHITE	Vertex not yet visited.GRAY	Vertex is on the current DFS recursion stack (ancestors of the current call).BLACK	Vertex and all its descendants have been completely processed.A back edge (an edge from a GRAY vertex to another GRAY vertex) indicates a cycle in a directed graph. When such an edge is found, the algorithm can stop immediately.ComplexityTime – O(V + E) – each vertex and edge is examined once.Space – O(V) for the color array and recursion stack (or explicit stack for iterative DFS).---Key ConceptsDFS explores a path to its deepest point before backtracking.The recursion stack implicitly represents the current path; the GRAY set is exactly that stack.In an undirected graph, a visited‑GRAY edge could be the parent edge, so a different rule is used.---Evaluation PointsProvide the three‑color DFS pseudocode.Explain why an edge to a GRAY vertex means a cycle.State O(V + E) time and O(V) space.---Common MistakesUsing only a visited boolean (WHITE/BLACK) – this only works for undirected graphs.Forgetting to reset colors before starting the next DFS component.---Follow‑up QuestionsHow would you modify the algorithm to return the actual cycle (list of vertices)?What changes are needed to detect a cycle in an undirected graph?Can you use Kahn’s topological sort to detect a cycle? How does its complexity compare?---Interview TipsBegin with “We run DFS and color vertices; a back edge to a gray vertex proves a cycle.”Emphasize that the algorithm works for any directed graph, even disconnected ones.---Tags  DSA, Graph, Cycle Detection, DFS, Coloring, MediumSourceCormen et al., Introduction to Algorithms, 3rd ed., Chapter 22.https://en.wikipedia.org/wiki/Depth-first_search#Vertex_coloring---Record TypeTechnical QuestionRole  Software Engineer / Mobile Developer / Algorithm EngineerCategory  TechnicalSkill  Data Structures & AlgorithmsSubtopic  Sliding Window TechniqueExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven a string `s`, find the length of the longest substring that contains no repeating characters. Explain the sliding‑window solution and its complexity.---Model AnswerAlgorithm (Sliding Window + hashmap)text maxLen = 0 left = 0 map = empty hash map // character → latest indexfor right = 0 … s.length‑1 c = s[right] if c exists in map and map[c] ≥ left // character repeats inside current window left = map[c] + 1 map[c] = right maxLen = max(maxLen, right – left + 1)return maxLenWhy it worksThe window `[left, right]` always contains unique characters.When a duplicate character `c` appears, we slide `left` just past the previous occurrence of `c`.The hashmap lets us jump directly to the last index of `c` (O(1) lookup).ComplexityTime – O(n) – each character is visited at most twice (once by `right`, once by moving `left`).Space – O(min(m, n)) where `m` is the size of the character set (hash map stores at most one entry per distinct character in the current window).---Key ConceptsMaintaining an invariant (window has all unique characters).Using a hash map for constant‑time index lookup.The technique generalises to any “longest subarray/substring with a constraint”.---Evaluation PointsCorrect sliding‑window loop with `left`/`right` pointers.Explain the update of `left` when a duplicate is found.State O(n) time, O(k) space (`k` = distinct characters).---Common MistakesForgetting to move `left` only forward (may cause infinite loop).Using `left = map[c]` instead of `+1`, which would keep the duplicate inside the window.---Follow‑up QuestionsHow would you adapt the algorithm to find the longest substring with at most k distinct characters?What is the time‑space trade‑off if you replace the hashmap with a fixed‑size array (e.g., for ASCII)?Can this technique be used for arrays of integers with a sum constraint?---Interview TipsState the invariant clearly: “The current window never contains duplicates.”Mention that the hashmap stores the most recent index, allowing `left` to jump forward in O(1).---Tags  DSA, Sliding Window, Substring, Hash Map, Linear Time, MediumSourceLeetCode “Longest Substring Without Repeating Characters”.https://www.geeksforgeeks.org/longest-substring-without-repeating-characters/---Record TypeTechnical QuestionRole  Software Engineer / Systems Engineer / Algorithm EngineerCategory  TechnicalSkill  Data Structures & AlgorithmsSubtopic  Disjoint Set Union (Union‑Find)Experience Level  Fresher, ExperiencedDifficulty  Hard---QuestionExplain the Union‑Find (Disjoint Set Union) data structure. How do path compression and union by rank work, and what is the resulting amortized time complexity of a sequence of m operations on n elements?---Model AnswerStructure`parent[i]` – points to the representative (root) of the set containing `i`.`rank[i]` (or size) – an upper bound on the height of the tree rooted at `i`.Find with Path Compressiontext function Find(x): if parent[x] != x: parent[x] = Find(parent[x]) // recursively collapse the path return parent[x]Each call flattens the path from `x` to its root, making future finds O(1) for those nodes.Union by Ranktext function Union(x, y): rx = Find(x) ry = Find(y) if rx == ry: return if rank[rx] < rank[ry]: parent[rx] = ry else if rank[rx] > rank[ry]: parent[ry] = rx else: parent[ry] = rx rank[rx] = rank[rx] + 1The root with the smaller rank becomes a child of the larger‑rank root, keeping tree height minimal.ComplexityA single `Find` or `Union` is amortized O(α(n)), where α is the inverse Ackermann function, which grows extremely slowly (α(10⁶) ≈ 4).Therefore a sequence of m operations on n elements runs in O(m · α(n)), practically linear.---Key ConceptsRepresent sets as rooted trees.Path compression makes every node on the find‑path point directly to the root.Union by rank (or size) keeps the tree shallow, guaranteeing the α(n) bound.Often used in Kruskal’s Minimum Spanning Tree algorithm and connectivity queries.---Evaluation PointsProvide both `Find` (with path compression) and `Union` (with rank) pseudocode.Explain why each heuristic reduces tree height.State the amortized O(α(n)) per operation (practically O(1)).---Common MistakesUsing only one heuristic (e.g., only path compression) – still works but the bound is weaker.Updating `rank` incorrectly (should increase only when two trees of equal rank are merged).---Follow‑up QuestionsHow would you modify Union‑Find to support deletions of elements?Can you implement Union‑Find iteratively without recursion?Apply Union‑Find to solve the Number of Islands problem on a 2‑D grid.---Interview TipsMention the classic “almost‑constant” time claim and that α(n) ≤ 5 for any realistic n.Give a quick example (e.g., merging sets `{1,2}` and `{3,4}`) to illustrate the rank update.---Tags  DSA, Union Find, Disjoint Set, Path Compression, Union by Rank, Amortized Analysis, HardSourceCormen et al., Introduction to Algorithms, 3rd ed., Chapter 21.https://en.wikipedia.org/wiki/Disjoint-set_data_structure---Record TypeTechnical QuestionRole  Java Developer / Backend Engineer / Software EngineerCategory  TechnicalSkill  Java, Data Structures & AlgorithmsSubtopic  Search Algorithms – Binary Search (Iterative)Experience Level  Fresher, ExperiencedDifficulty  Easy---QuestionWrite an iterative binary‑search method in Java that returns the index of a target value in a sorted `int[]`. If the target is not present, return `-1`. Also state its time and space complexities.---Model Answerjava public class BinarySearch { /** * Returns the index of {@code target} in {@code arr} or -1 if not found. * {@code arr} must be sorted in ascending order. */ public static int search(int[] arr, int target) { int low = 0; int high = arr.length - 1;    while (low &lt;= high) {        // avoid overflow: use low + (high - low) / 2        int mid = low + ((high - low) &gt;&gt;&gt; 1);        if (arr[mid] == target) {            return mid;               // found        } else if (arr[mid] &lt; target) {            low = mid + 1;            // search right half        } else {            high = mid - 1;           // search left half        }    }    return -1;                       // not found}}ComplexitiesMetric	ValueTime	`O(log n)` – each loop halves the remaining interval.Space	`O(1)` – only a few integer variables are used (iterative version).---Key ConceptsWorks only on a sorted array; the algorithm is a classic divide‑and‑conquer technique.The mid‑point calculation uses unsigned right shift (`>>>`) to prevent integer overflow.Iterative version avoids the `O(log n)` recursion stack overhead.---Evaluation PointsCorrect Java method signature and return type.Proper loop condition (`low <= high`) and mid calculation.Clear statement of `O(log n)` time and `O(1)` space.---Common MistakesUsing `(low + high) / 2` which can overflow for large indices.Forgetting to update `low` or `high` correctly, causing an infinite loop.Attempting binary search on an unsorted array (the method should assume the pre‑condition).---Follow‑up QuestionsHow would you modify the method to return the insertion point (the index where the target should be inserted to keep the array sorted)?What changes are needed for a recursive implementation, and what is its space complexity?Can you use binary search to find the first or last occurrence of a duplicate value in a sorted array?---Interview TipsBegin with a one‑sentence definition: “Binary search repeatedly halves a sorted range to locate a target in `O(log n)` time.”Emphasize the overflow‑safe mid calculation; interviewers love to see that you know the subtle bug.Mention that Java’s `Arrays.binarySearch` does exactly this, but you should be able to code it yourself.---Tags  Java, Binary Search, Search Algorithms, Time Complexity, Space Complexity, EasySourceCormen, Leiserson, Rivest, Stein – Introduction to Algorithms, 3rd ed., Chapter 9.Java SE 8 API – `java.util.Arrays.binarySearch`.---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Front‑End Developer / Mobile EngineerCategory  TechnicalSkill  Arrays, HashMap, Algorithm DesignSubtopic  Two‑Sum (Hash‑Map solution)Experience Level  Fresher, ExperiencedDifficulty  Easy---QuestionGiven an integer array `nums` and an integer `target`, return the indices of the two numbers such that they add up to `target`.  Assume exactly one solution exists and you may not use the same element twice.Model Answer (Java)java public int[] twoSum(int[] nums, int target) { Map map = new HashMap<>(); // value → index for (int i = 0; i < nums.length; i++) { int complement = target - nums[i]; if (map.containsKey(complement)) { return new int[]{map.get(complement), i}; } map.put(nums[i], i); } throw new IllegalArgumentException("No two sum solution"); }ComplexitiesMetric	ValueTime	O(n) – one pass through the array.Space	O(n) – hash map stores at most n entries.---Key ConceptsUse a hash map to store previously‑seen numbers and their indices.For each element, check whether its complement (`target – nums[i]`) is already in the map.---Evaluation PointsCorrect use of `HashMap`.Returns the two indices in any order.Handles the “exactly one solution” guarantee without extra checks.---Common MistakesUsing a nested loop → O(n²) time.Forgetting to check `map.containsKey(complement)` before inserting the current element (would return the same index).---Follow‑up QuestionsHow would you modify the solution to return all unique pairs?What if the input array is sorted – can you solve it with O(1) extra space?How would you adapt the algorithm for the Three‑Sum problem?---Interview TipsStart by stating the brute‑force O(n²) idea, then explain why a hash map gives O(n).Mention that the method throws an exception if no solution is found – a defensive practice.---Tags  Arrays, HashMap, Two Sum, Java, Easy, InterviewSourceLeetCode “Two Sum”.https://www.geeksforgeeks.org/two-sum-problem/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Mobile EngineerCategory  TechnicalSkill  String Manipulation, Sliding Window, HashMapSubtopic  Sliding‑Window TechniqueExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven a string `s`, find the length of the longest substring that contains no repeating characters.Model Answer (Java)java public int lengthOfLongestSubstring(String s) { int n = s.length(); int max = 0; Map lastPos = new HashMap<>(); int left = 0; // start of current windowfor (int right = 0; right < n; right++) {char c = s.charAt(right);if (lastPos.containsKey(c) && lastPos.get(c) >= left) {left = lastPos.get(c) + 1;   // move left past previous occurrence}lastPos.put(c, right);max = Math.max(max, right - left + 1);}return max;}ComplexitiesMetric	ValueTime	O(n) – each character is visited at most twice.Space	O(min(m, n)) – `m` = size of the character set (hash map).---Key ConceptsMaintain a sliding window `[left, right]` that always has unique characters.Hash map stores the latest index of each character, allowing the left pointer to jump forward instantly.---Evaluation PointsCorrect sliding‑window loop with proper update of `left`.Use of `HashMap` for O(1) look‑ups.Proper handling of edge cases (empty string).---Common MistakesUpdating `left = lastPos.get(c)` instead of `+1`, which leaves the duplicate inside the window.Not checking `lastPos.get(c) >= left` – would move `left` backwards.---Follow‑up QuestionsExtend the solution to allow at most k distinct characters.What is the time‑space trade‑off if you replace the `HashMap` with an `int[128]` array for ASCII strings?How would you output the actual substring, not just its length?---Interview TipsClearly state the invariant: “Current window has no duplicates.”Emphasize that the algorithm is linear because each index moves only forward.---Tags  String, Sliding Window, HashMap, Java, Medium, InterviewSourceLeetCode “Longest Substring Without Repeating Characters”.https://www.geeksforgeeks.org/longest-substring-without-repeating-characters/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Mobile EngineerCategory  TechnicalSkill  Linked List, Pointer ManipulationSubtopic  Iterative Reversal of a Singly Linked ListExperience Level  Fresher, ExperiencedDifficulty  Easy---QuestionWrite a method `ListNode reverseList(ListNode head)` that reverses a singly linked list and returns the new head node.java class ListNode { int val; ListNode next; ListNode(int x) { val = x; } }Model Answer (Iterative)java public ListNode reverseList(ListNode head) { ListNode prev = null; ListNode curr = head; while (curr != null) { ListNode nextTmp = curr.next; // store next node curr.next = prev; // reverse pointer prev = curr; // move prev forward curr = nextTmp; // move curr forward } return prev; // prev becomes the new head }ComplexitiesMetric	ValueTime	O(n) – one pass through the list.Space	O(1) – only three pointer variables.---Key ConceptsIn‑place reversal using three pointers (`prev`, `curr`, `next`).After the loop, `prev` points to the new head; `curr` is `null`.---Evaluation PointsNo extra data structures (constant space).Handles empty list (`head == null`).---Common MistakesForgetting to store `curr.next` before re‑assigning it (loses the rest of the list).Returning `head` instead of `prev`.---Follow‑up QuestionsHow would you reverse the list recursively? What is its space complexity?Modify the algorithm to reverse the list in groups of k nodes.How can you detect whether a linked list has a cycle before reversing it?---Interview TipsMention the three‑pointer pattern before writing code.Emphasize that the method works for any length, including 0 or 1.---Tags  Linked List, Reverse, Java, O(1) Space, Easy, InterviewSourceCracking the Coding Interview, 6th ed., Chapter 2.https://leetcode.com/problems/reverse-linked-list/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / System EngineerCategory  TechnicalSkill  Stack, Queue, Data‑Structure DesignSubtopic  Queue using Two Stacks (Amortized O(1) operations)Experience Level  Fresher, ExperiencedDifficulty  Medium---QuestionDesign a class `MyQueue` that implements a FIFO queue using only two stacks. Provide the methods `void push(int x)`, `int pop()`, `int peek()`, and `boolean empty()`.Model Answer (Java)java class MyQueue { private Deque inbox = new ArrayDeque<>(); private Deque outbox = new ArrayDeque<>();/** Push element x to the back of queue. */public void push(int x) {inbox.push(x);               // O(1)}/** Move elements to outbox only when needed. */private void shiftStacks() {if (outbox.isEmpty()) {while (!inbox.isEmpty()) {outbox.push(inbox.pop());}}}/** Remove the element from front of queue and return it. */public int pop() {shiftStacks();return outbox.pop();          // O(1) amortized}/** Get the front element. */public int peek() {shiftStacks();return outbox.peek();         // O(1) amortized}/** Return true if the queue is empty. */public boolean empty() {return inbox.isEmpty() && outbox.isEmpty();}}ComplexitiesOperation	Amortized Time`push`	O(1)`pop`	O(1)`peek`	O(1)`empty`	O(1)(The costly transfer from `inbox` to `outbox` happens at most once for each element.)---Key ConceptsTwo stacks simulate queue behavior: `inbox` receives pushes, `outbox` provides pops.Transfer only when `outbox` is empty → amortized constant time.---Evaluation PointsUse of `Deque<Integer>` (or `Stack<Integer>`) for stack operations.Correct `shiftStacks` logic to avoid unnecessary moves.---Common MistakesTransferring elements on every `pop` (still O(1) but unnecessary work).Forgetting to check `outbox.isEmpty()` before moving elements.---Follow‑up QuestionsHow would you implement a stack using two queues?What is the worst‑case time for a single `pop` operation?Can you extend the design to support a max‑queue (constant‑time retrieval of the maximum element)?---Interview TipsExplain the intuition first: “push → inbox, pop → outbox; when outbox is empty, dump inbox into it.”Mention that the amortized analysis relies on each element moving at most once from inbox to outbox.---Tags  Stack, Queue, Two Stacks, Amortized Analysis, Java, Medium, InterviewSourceLeetCode “Implement Queue using Stacks”.https://www.geeksforgeeks.org/implement-queue-using-stacks/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Data EngineerCategory  TechnicalSkill  Binary Tree, RecursionSubtopic  Binary Search Tree (BST) ValidationExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven the root of a binary tree, write a method `boolean isValidBST(TreeNode root)` that returns true if the tree satisfies the binary search tree property.java class TreeNode { int val; TreeNode left; TreeNode right; TreeNode(int x) { val = x; } }Model Answer (Recursive Range‑Check)java public boolean isValidBST(TreeNode root) { return validate(root, Long.MIN_VALUE, Long.MAX_VALUE); }private boolean validate(TreeNode node, long low, long high) { if (node == null) return true; if (node.val <= low || node.val >= high) return false; return validate(node.left, low, node.val) && validate(node.right, node.val, high); }ComplexitiesMetric	ValueTime	O(n) – each node visited once.Space	O(h) – recursion stack, where h is tree height (worst‑case O(n)).---Key ConceptsUse inclusive/exclusive bounds (`low < node.val < high`).Pass `Long.MIN_VALUE`/`Long.MAX_VALUE` to avoid overflow when node values are `Integer.MIN_VALUE`/`MAX_VALUE`.---Evaluation PointsCorrect handling of the lower/upper bounds.No reliance on in‑order traversal list (which also works but uses extra O(n) space).---Common MistakesUsing `node.val <= low || node.val >= high` incorrectly (should be `<`/`>` depending on the chosen convention).Forgetting to update bounds when recursing to the right subtree (`low = node.val`).---Follow‑up QuestionsHow would you validate a BST iteratively using a stack?Can you adapt the algorithm to handle duplicate values where duplicates are allowed only on the right subtree?What is the time‑space trade‑off between the recursive solution and an in‑order traversal that checks monotonicity?---Interview TipsStart with the definition: “For every node, all values in the left subtree must be smaller, all in the right subtree larger.”Emphasize the importance of passing the range down the recursion.---Tags  Binary Tree, BST Validation, Recursion, Java, Medium, InterviewSourceLeetCode “Validate Binary Search Tree”.https://www.geeksforgeeks.org/validate-binary-search-tree/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Data Engineer / Backend EngineerCategory  TechnicalSkill  Heap, PriorityQueue, Selection AlgorithmsSubtopic  Finding K Smallest / Largest ElementsExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven an unsorted integer array `arr` and an integer `k`, return the k smallest elements in any order. Aim for O(n log k) time and O(k) extra space.Model Answer (Java – Max‑Heap of size k)java public List kSmallest(int[] arr, int k) { if (k <= 0) return Collections.emptyList(); PriorityQueue maxHeap = new PriorityQueue<>(Collections.reverseOrder());for (int num : arr) {if (maxHeap.size() < k) {maxHeap.offer(num);} else if (num < maxHeap.peek()) {maxHeap.poll();          // remove largest among the k keptmaxHeap.offer(num);}}return new ArrayList<>(maxHeap); // contains the k smallest numbers}ComplexitiesMetric	ValueTime	O(n log k) – each insertion/removal from a heap of size k costs log k.Space	O(k) – the heap stores only k elements.---Key ConceptsUse a max‑heap (priority queue with reverse order) to keep the current k smallest numbers.When a new element is smaller than the heap’s maximum, replace the maximum.---Evaluation PointsCorrect creation of a max‑heap (`Collections.reverseOrder()`).Proper handling of edge cases (`k == 0`, `k > arr.length`).---Common MistakesUsing a min‑heap and extracting k times – results in O(n + k log n) and extra work.Forgetting to check `k > arr.length` (could just return the whole array).---Follow‑up QuestionsHow would you solve the problem using the QuickSelect algorithm (average O(n) time)?If the input stream is infinite, how can you maintain the k smallest elements on the fly?What changes are required to return the k largest elements instead?---Interview TipsMention the trade‑off: heap solution is simple and guarantees O(n log k); QuickSelect is faster on average but more complex.---Tags  Heap, PriorityQueue, K Smallest, Java, Medium, InterviewSourceCormen et al., Introduction to Algorithms, Chapter 9 (selection).https://www.geeksforgeeks.org/k-smallest-elements/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Data EngineerCategory  TechnicalSkill  Graph Algorithms, BFSSubtopic  Breadth‑First Search, Shortest Path (Unweighted)Experience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven an undirected, unweighted graph represented as an adjacency list, write a method `int shortestPath(int src, int dst)` that returns the minimum number of edges between `src` and `dst`. Return `-1` if `dst` is unreachable.Model Answer (Java)java public int shortestPath(int src, int dst, List> adj) { if (src == dst) return 0;int n = adj.size();boolean[] visited = new boolean[n];int[] dist = new int[n];Queue q = new ArrayDeque<>();visited[src] = true;q.offer(src);while (!q.isEmpty()) {int u = q.poll();for (int v : adj.get(u)) {if (!visited[v]) {visited[v] = true;dist[v] = dist[u] + 1;if (v == dst) return dist[v];q.offer(v);}}}return -1; // unreachable}ComplexitiesMetric	ValueTime	O(V + E) – each vertex and edge examined once.Space	O(V) – for `visited`, `dist`, and the BFS queue.---Key ConceptsBFS explores vertices level by level; the first time we reach `dst` we have the shortest path.Use an adjacency list (`List<List<Integer>>`) for efficient traversal.---Evaluation PointsProper initialization of `visited` and `dist`.Early exit when `dst` is dequeued (or discovered).---Common MistakesUsing DFS – would not guarantee shortest path in an unweighted graph.Forgetting to mark a node as visited before enqueueing it (causes duplicate visits).---Follow‑up QuestionsHow would you reconstruct the actual path (list of vertices) instead of just its length?Extend the algorithm to handle weighted edges (Dijkstra’s algorithm).What changes are needed for a directed graph?---Interview TipsState the BFS invariant: “All nodes at distance d are processed before any node at distance d + 1.”Mention that the algorithm works for both connected and disconnected graphs.---Tags  Graph, BFS, Shortest Path, Java, Medium, InterviewSourceCLRS (Cormen) Chapter 22 – Breadth‑First Search.https://www.geeksforgeeks.org/breadth-first-search-or-bfs-for-a-graph/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Mobile Engineer / Backend EngineerCategory  TechnicalSkill  Dynamic ProgrammingSubtopic  DP – Fibonacci‑type recurrence (Climbing Stairs)Experience Level  Fresher, ExperiencedDifficulty  Easy---QuestionYou are climbing a staircase. It takes 1 or 2 steps to reach the next stair.  Given `n` stairs, how many distinct ways can you climb to the top?  Implement `int climbStairs(int n)`.Model Answer (Java – O(1) space)java public int climbStairs(int n) { if (n <= 2) return n; // base cases int a = 1; // ways to reach step i‑2 int b = 2; // ways to reach step i‑1 int cur = 0; for (int i = 3; i <= n; i++) { cur = a + b; // DP recurrence: f(i) = f(i‑1) + f(i‑2) a = b; b = cur; } return b; }ComplexitiesMetric	ValueTime	O(n) – single loop.Space	O(1) – only three integer variables.---Key ConceptsThe problem is identical to computing the nth Fibonacci number with different base cases.DP recurrence: `f(i) = f(i‑1) + f(i‑2)`.---Evaluation PointsCorrect base cases (`n = 1 → 1`, `n = 2 → 2`).Linear loop with constant extra space.---Common MistakesUsing exponential recursion without memoization (`O(2ⁿ)` time).Returning `a` instead of `b` after the loop.---Follow‑up QuestionsHow would you solve the problem modulo 10⁹ + 7 for very large `n`?Generalize the solution to allow k steps at a time.Show how to compute the Model Answer in O(log n) time using matrix exponentiation.---Interview TipsMention the naive recursive solution first, then improve it with DP (memoization → bottom‑up).Highlight the space‑optimised version (only two previous values are needed).---Tags  Dynamic Programming, Fibonacci, Climbing Stairs, Java, Easy, InterviewSourceLeetCode “Climbing Stairs”.https://www.geeksforgeeks.org/number-ways-reach-nth-stair/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Data EngineerCategory  TechnicalSkill  Greedy Algorithms, SortingSubtopic  Maximum Set of Non‑Overlapping IntervalsExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven a list of activities with start and end times, select the maximum number of activities that can be performed by a single person, assuming that a person can only work on one activity at a time.Define `int maxActivities(int[][] intervals)` where each `interval[i] = {start, end}`.Model Answer (Java – Greedy on earliest finish time)java public int maxActivities(int[][] intervals) { if (intervals == null || intervals.length == 0) return 0;// Sort by end time (ascending)Arrays.sort(intervals, (a, b) -> Integer.compare(a[1], b[1]));int count = 1;                     // pick the first activityint lastEnd = intervals[0][1];for (int i = 1; i < intervals.length; i++) {if (intervals[i][0] >= lastEnd) {   // compatiblecount++;lastEnd = intervals[i][1];}}return count;}ComplexitiesMetric	ValueTime	O(n log n) – sorting dominates.Space	O(1) additional (in‑place sort).---Key ConceptsGreedy choice: always pick the activity that finishes earliest among the remaining ones.After picking an activity, discard all that overlap with it and repeat.---Evaluation PointsProper sorting by the end time, not start time.Correct compatibility check (`start >= lastEnd`).---Common MistakesSorting by start time – leads to sub‑optimal selection.Using `>` instead of `>=`, which incorrectly rejects back‑to‑back activities.---Follow‑up QuestionsHow would you modify the algorithm to return the actual set of selected intervals?If each activity also has a profit, does the greedy approach still work? (No – need DP).What is the time‑space complexity if the intervals are already sorted by end time?---Interview TipsState the “earliest finish” greedy rule before coding.Emphasize that the greedy proof hinges on the fact that choosing the earliest‑finishing activity leaves the most room for the remaining ones.---Tags  Greedy, Activity Selection, Sorting, Java, Medium, InterviewSourceCLRS Chapter 16 – Greedy Algorithms.https://www.geeksforgeeks.org/activity-selection-problem-greedy-algo-1/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Mobile Engineer / Backend EngineerCategory  TechnicalSkill  Backtracking, RecursionSubtopic  Permutation GenerationExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionWrite a method `List<String> permute(String s)` that returns all permutations of the characters in `s`. The order of the permutations does not matter.Model Answer (Java – Backtracking with swapping)java public List permute(String s) { List result = new ArrayList<>(); char[] arr = s.toCharArray(); backtrack(arr, 0, result); return result; }private void backtrack(char[] arr, int idx, List res) { if (idx == arr.length) { res.add(new String(arr)); return; } for (int i = idx; i < arr.length; i++) { swap(arr, idx, i); // choose backtrack(arr, idx + 1, res); // explore swap(arr, idx, i); // un‑choose (backtrack) } }private void swap(char[] a, int i, int j) { char tmp = a[i]; a[i] = a[j]; a[j] = tmp; }ComplexitiesMetric	ValueTime	O(n · n!) – n! permutations, each of length n is built.Space	O(n) – recursion stack + temporary char array. (Output list itself is O(n · n!)).---Key ConceptsBacktracking: choose a character, recurse, then undo the choice (swap back).Swapping in‑place avoids extra copies, keeping the auxiliary space linear.---Evaluation PointsCorrect recursive structure with base case `idx == arr.length`.Proper swap before and after the recursive call (backtrack).---Common MistakesForgetting to swap back, which corrupts later branches.Using a new string at each recursion step – leads to O(n²) extra memory per call.---Follow‑up QuestionsHow would you modify the algorithm to handle duplicate characters and avoid duplicate permutations?Generate all subsets (power set) of a string – what changes in the backtracking approach?What is the time complexity if you only need the k‑th permutation in lexicographic order?---Interview TipsMention the “choose → explore → un‑choose” pattern.Explain why swapping works (it keeps the current prefix fixed while exploring the remaining suffix).---Tags  Backtracking, Permutation, Recursion, Java, Medium, InterviewSourceLeetCode “Permutations”.https://www.geeksforgeeks.org/permutations-of-a-given-string/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Systems Engineer / Embedded EngineerCategory  TechnicalSkill  Bit ManipulationSubtopic  Counting Set BitsExperience Level  Fresher, ExperiencedDifficulty  Easy---QuestionWrite a method `int countBits(int n)` that returns the number of 1‑bits (set bits) in the binary representation of a non‑negative integer `n`.Model Answer (Java – Brian Kernighan’s Algorithm)java public int countBits(int n) { int count = 0; while (n != 0) { n &= (n - 1); // clear the least‑significant set bit count++; } return count; }ComplexitiesMetric	ValueTime	O(k) where k = number of set bits (worst case O(32) for `int`).Space	O(1).---Key Concepts`n & (n‑1)` drops the lowest‑order `1` bit, so the loop runs exactly once per set bit.Works for any integer size (use `long` for 64‑bit values).---Evaluation PointsCorrect use of `n &= (n‑1)` inside the loop.Returns `0` for `n = 0`.---Common MistakesUsing `n >>= 1` and checking the LSB – still O(32) but less efficient; acceptable but not the intended “clever” solution.Forgetting to handle negative numbers (Java’s signed right shift keeps the sign bit).---Follow‑up QuestionsHow would you compute the parity (odd/even number of set bits) in O(1) using bitwise tricks?Implement `int reverseBits(int n)` that returns the bits of `n` reversed.What is the built‑in Java method that does the same job? (`Integer.bitCount`).---Interview TipsMention the intuition: each iteration removes one `1`, so the loop count equals the number of set bits.If asked for a one‑liner, point out `Integer.bitCount(n)` (Java 8+).---Tags  Bit Manipulation, Set Bits, Brian Kernighan, Java, Easy, InterviewSourceWikipedia “Brian Kernighan’s algorithm”.https://www.geeksforgeeks.org/count-set-bits-in-an-integer/---Record TypeTechnical QuestionRole  Software Engineer / Java Developer / Backend Engineer / Data EngineerCategory  TechnicalSkill  Recursion, BacktrackingSubtopic  Generating Power SetExperience Level  Fresher, ExperiencedDifficulty  Medium---QuestionGiven an integer array `nums` (no duplicates), write a method `List<List<Integer>> subsets(int[] nums)` that returns all possible subsets (the power set).Model Answer (Java – Recursive Backtracking)java public List> subsets(int[] nums) { List> result = new ArrayList<>(); backtrack(0, nums, new ArrayList<>(), result); return result; }private void backtrack(int index, int[] nums, List path, List> res) { // add current subset res.add(new ArrayList<>(path));for (int i = index; i < nums.length; i++) {path.add(nums[i]);                 // choosebacktrack(i + 1, nums, path, res); // explorepath.remove(path.size() - 1);      // un‑choose}}ComplexitiesMetric	ValueTime	O(n · 2ⁿ) – there are 2ⁿ subsets, each of average size n/2.Space	O(n) recursion stack + O(2ⁿ) for the output list.---Key ConceptsAt each index you have two choices: include the element or skip it.The recursion depth equals the number of elements `n`.---Evaluation PointsCorrect base case (adding the current `path` to `result`).Proper backtracking (`remove` after the recursive call).---Common MistakesAdding the same `path` reference to `result` without copying – later modifications corrupt earlier subsets.Forgetting to start the loop from `index` (causes duplicate subsets).---Follow‑up QuestionsHow would you generate subsets iteratively using bit masks?If the input may contain duplicate numbers, how do you avoid duplicate subsets?What is the total number of subsets for an array of length `n`? (Model Answer: 2ⁿ)---Interview TipsMention the “binary‑mask” interpretation (each element either present or absent) before presenting the recursive solution.Emphasize the need to copy the current subset (`new ArrayList<>(path)`) before storing it.---Tags  Recursion, Subsets, Backtracking, Java, Medium, InterviewSourceLeetCode “Subsets”.https://www.geeksforgeeks.org/power-set-using-recursion/---